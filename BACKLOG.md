# Backlog

1. [x] Scaffolding & health endpoint — Create .NET 10 Minimal API project (LogViewerApi.csproj) with NuGet packages (Azure.Identity, Azure.Storage.Blobs, Microsoft.AspNetCore.OpenApi), Program.cs with BlobServiceClient DI registration via DefaultAzureCredential and STORAGE_ACCOUNT_URL env var, OpenAPI doc at /openapi/v1.json and Swagger UI at /swagger, global exception handler mapping RequestFailedException to consistent {"error":"..."} JSON, GET /health returning {"status":"ok"}, directory structure (Services/, Models/, Endpoints/), empty IBlobStorageService interface and BlobStorageService class registered in DI, xUnit test project with placeholder test <!-- depends: none -->
2. [1] List projects & runs — Implement BlobStorageService methods to list blob containers as projects and top-level virtual-folder prefixes as runs, create response DTOs (ProjectListResponse, RunListResponse with id and last_modified fields), implement GET /projects and GET /projects/{projectId}/runs endpoints, return 404 {"error":"Project not found"} when container doesn't exist <!-- depends: 1 -->
3. [ ] List run logs — Implement BlobStorageService method to list and classify blobs within a run prefix: agent logs (*.log files), prompts (blobs under prompts/ sub-prefix), artifacts (everything else including events.jsonl, run-metadata.json, validation-*.txt, analysis-*.txt, *.done), create categorized LogListResponse DTOs with name/size/last_modified per item, implement GET /projects/{projectId}/runs/{runId}/logs endpoint, return 404 {"error":"Run not found"} when no blobs match the run prefix <!-- depends: 1 -->
4. [ ] Log content retrieval & tail — Implement BlobStorageService methods for blob content download (full and offset-based range reads) and tail (last N lines without full download), create content response DTOs, implement GET /projects/{projectId}/runs/{runId}/logs/{fileName} with raw (bool, default false) and offset (long, default 0) query params returning JSON envelope by default or raw content with Content-Type and Content-Range headers, implement GET /projects/{projectId}/runs/{runId}/logs/{fileName}/tail with lines (int, default 100) query param, return 404 {"error":"Log not found"} when blob doesn't exist <!-- depends: 1 -->
5. [ ] Dockerfile & Kubernetes manifests — Create multi-stage Dockerfile using mcr.microsoft.com/dotnet/sdk:10.0 for build and mcr.microsoft.com/dotnet/aspnet:10.0 for runtime exposing port 8080, create Kubernetes Deployment manifest (single replica, buildteam-sa service account, azure.workload.identity/use: "true" pod label, STORAGE_ACCOUNT_URL env var, liveness and readiness probes on /health) and Service manifest (port 80 targeting container port 8080) <!-- depends: 1 -->
